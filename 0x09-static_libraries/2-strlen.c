#include "main.h"
#include <string.h>
/**
 * _strlen - that returns the length of a string
 * @s: string to char
 * @strlen(): return length of char
 * Return: length of the string (int)
 */
int _strlen(char *s)
{
	int str_length;

	str_length = strlen(s);

	return (str_length);
}
